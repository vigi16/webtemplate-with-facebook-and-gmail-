'use strict';

describe('myApp.gmail module', function() {

  beforeEach(module('myApp.gmail'));

  describe('gmail controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('GmailCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});