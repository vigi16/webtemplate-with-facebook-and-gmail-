'use strict';

describe('myApp.facebook module', function() {

  beforeEach(module('myApp.facebook'));

  describe('facebook controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('FacebookCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});